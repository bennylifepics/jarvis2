# jarvis2

Exemplo de ficheiro HTML com persistência de dados:

- `inventory-tracker.html`

Funcionalidades implementadas:

- Guarda dados no `localStorage` automaticamente.
- Exporta os dados para ficheiro `JSON`.
- Importa dados a partir de ficheiro `JSON`.
